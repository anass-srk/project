apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: route-db-data
  name: route-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
