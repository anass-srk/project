apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: subscription-db-data
  name: subscription-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
