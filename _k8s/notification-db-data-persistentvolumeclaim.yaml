apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: notification-db-data
  name: notification-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
